package Projects.JVM;

import java.util.Random;

/**
 * @author Cube
 * @className JVMAdjust
 * @description
 * @date 2019-06-03 17:06
 */
public class JVMAdjust {
    public static void main(String[] args) {
        // long maxMemory = Runtime.getRuntime().maxMemory();
        // long defaultMemory = Runtime.getRuntime().totalMemory();
        // System.out.println("maxMemory = " + maxMemory + "字节，" + (maxMemory / 1024 / 1024) + "MB");
        // System.out.println("defaultMemory = " + defaultMemory + "字节，" + (defaultMemory / 1024 / 1024) + "MB");

        // String str = "OutOfMemoryError:Java Heap space";
        // while (true){
        //     str += str + new Random().nextInt(1021202) + new Random().nextInt(9890009);
        // }
    }
}
